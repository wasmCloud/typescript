{
  "name": "http-api-with-distributed-workloads",
  "version": "0.1.0",
  "description": "A wasmCloud template demonstrating HTTP API with distributed workloads via wasmcloud:messaging",
  "private": true,
  "type": "module",
  "scripts": {
    "setup:wit": "npm run setup:wit --workspace=http-api && npm run setup:wit --workspace=task-leet && npm run setup:wit --workspace=task-reverse",
    "build": "npm run build --workspace=http-api && npm run build --workspace=task-leet && npm run build --workspace=task-reverse",
    "dev": "nodemon"
  },
  "workspaces": [
    "http-api",
    "task-leet",
    "task-reverse"
  ],
  "devDependencies": {
    "nodemon": "^3.1.9"
  }
}
