{
  "name": "http-client",
  "version": "0.1.0",
  "description": "A WebAssembly component that makes outgoing HTTP requests using the fetch API",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "fetch:wit": "wkg wit fetch",
    "build:ts": "tsc",
    "build:component": "jco componentize -w wit -o dist/http_client.wasm dist/index.js",
    "build": "npm run fetch:wit && npm run build:ts && npm run build:component"
  },
  "keywords": [],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "@bytecodealliance/jco": "^1.15.4",
    "typescript": "^5.9.3"
  }
}
